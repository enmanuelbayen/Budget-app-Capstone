<div class="bg-primary text-white w-full h-14 flex place-items-center justify-between px-4">
  <% if left_button == 'Logout' %>
     <%= link_to 'Log Out', destroy_user_session_path, 'data-turbo-method': :delete %>
  <% elsif left_button.present? %>
  <%= link_to 'Back', left_button %>
  <% end %>
  <h2><%= navbar_title %></h2>
  <% if right_button == 'Search' %>
     <%= link_to 'Search' %>
  <% elsif right_button %>
     <%= right_button %>
  <% end %>
</div>

<p class="ml-2 text-blue-400"><%= notice %></p>
<p class="ml-2 text-red-400"><%= alert %></p>